# USDA FoodData Central API key (free): https://fdc.nal.usda.gov/api-key-signup.html
# Without this, the recommender uses a small offline food table so the pipeline
# still runs end to end.
FDC_API_KEY=

# Optional: path to the tesseract binary if it is not on PATH.
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
